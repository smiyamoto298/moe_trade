# Node（どの階層でも除外。ルートには vitest 実行時のキャッシュが生成されることがある）
node_modules/

# Laravel
backend/.env
backend/vendor/
backend/storage/logs/*
backend/storage/framework/cache/*
backend/storage/framework/sessions/*
backend/storage/framework/views/*

# React
frontend/dist/
frontend/.env.local

# Docker
docker/mysql/
# ルートの .env は worktree ごとの compose 設定（プロジェクト名・ポート）。雛形は .env.example
/.env

# OS
.DS_Store
Thumbs.db

# fansite
fansitekit/