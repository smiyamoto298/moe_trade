# .claude/shared_paths.txt
# 【フォールバック専用】標準フローは worktree 分離(別チェックアウト)で衝突を防ぐため、
# このロック一覧は使わない。worktree を使わず単一ツリーで複数編集する場合にのみ、
# `bash .claude/lock.sh acquire/release/check` を手動で使うときの共有ファイル一覧。
# 複数タスクが同時に触りうる「共有ファイル」を1行1パターンで列挙する(部分一致)。
# pre/post edit の自動ロック hook は settings.json から撤去済み(worktree が標準のため)。
#
# 行頭 # はコメント。空行は無視。
#
# --- moe_trade で衝突しやすい代表例(実在に合わせて調整) ---
backend/routes/api.php
backend/routes/web.php
frontend/src/App.tsx
design.md
# 共通ユーティリティ/型は同時編集が起きやすい
frontend/src/utils/itemType.ts
backend/app/Support/
